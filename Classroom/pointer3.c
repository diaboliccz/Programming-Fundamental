/*
 จงเขียนโปรแกรมรับตัวอักษร 1 ชุด แล้วแสดงผลลัพธ์จากหลังมาหน้า (Level 3)*/
#include <stdio.h>
#include <string.h>
int main(){
    char str[100];
    scanf("%s",str);
    char *p=str;
    int count = 1;
    int num=len(str);
    while(*p!='\0'){
        for(int i=0;i<num;i++){
            if(str[i]==)
        }
    }
    return 0;
}