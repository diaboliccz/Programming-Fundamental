/*
 จงเขียนโปรแกรมรับตัวอักษร 1 ชุด แล้วแสดงผลลัพธ์จากหลังมาหน้า (Level 3)*/
#include <stdio.h>
int main(){
    char str[100];
    char *tail;
    tail = &str;

    char *head;
    head = &str;
    scanf("%s",str);
    while(*tail!='\0'){
        
    }
    return 0;
}